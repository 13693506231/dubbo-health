<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.kk.dao.CheckItemDao">
    <insert id="addItem" parameterType="com.kk.pojo.CheckItem">
        insert into t_checkitem (code, name, sex, age, price, type, attention, remark) values (#{code},#{name},#{sex},#{age},#{price},#{type},#{attention},#{remark})
    </insert>

    <select id="selectByCondition" resultType="com.kk.pojo.CheckItem" parameterType="string">
        select * from t_checkitem
        <if test="queryString != null  and queryString.length > 0 ">
            where name like concat("%",#{queryString},"%") or code  like concat("%",#{queryString},"%")
        </if>
    </select>
</mapper>

